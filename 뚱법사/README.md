# 뚱법사 (법사 MP 턴사냥)

- **버전:** 이 폴더의 `version.json`
- **실행파일:** GitHub Release `ddungmage.exe` (또는 `뚱법사.exe`)
- **태그:** `v0.x.x` (헌터 `v1.4.x` 와 구분)
- **펌웨어:** 상위 `firmware/뚱힐러.hex` (프로토콜 동일)

자동업데이트 URL:

`https://raw.githubusercontent.com/blacknut0319-del/systemupdate/main/뚱법사/version.json`
