---
description: Implement exactly what is requested, no unrequested "improvements"
alwaysApply: true
---

# 이 프로젝트에서 지킬 것

- 사용자가 요청한 것만 정확히, 직관적으로 구현한다. "이게 더 좋을 것 같다",
  "이런 경우도 있을 것 같다" 같은 임의의 추측/개선을 코드에 마음대로 추가하지 않는다.
- 특히 HP%/ROI 판정 로직처럼 이미 여러 번 시행착오를 겪은 부분은, 검증된 옛날
  버전(`C:\Users\Administrator\Desktop\헤르메스코딩폴더\final\06291938\06191252.py`)의
  로직이 있다면 그걸 그대로 가져다 쓴다. 알고리즘을 새로 고안하거나 "더 정교하게"
  바꾸려 하지 않는다.
- 확신 없는 원인 추측을 코드에 바로 반영하지 말고, 먼저 사용자에게 확인하거나
  실제 데이터(로그/스크린샷)로 검증한다.
- 자잘한 파생 개선(디바운스, 상대밝기 비교, 행 자동탐지 등)을 스스로 판단해서
  추가하지 않는다 — 이런 식의 임의 추가가 반복적으로 새 버그를 만들었었다.
